import s from '@styles/template/header.module.css'

interface Props {

}

export default function Header(prosp: Props) {
    
    return (
        <div className={s.header}>
        <header></header>
        
        </div>
    )
}